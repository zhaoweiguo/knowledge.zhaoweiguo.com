ipcs
####

* ipcs命令的作用是报告进程间通信（IPC）设施的状态信息。具体来说，该命令可以向标准输出写入关于活动进程间通信设施的信息，如消息队列、共享内存段、信号量、远程队列和本地队列等。

ipcs命令的语法支持多种选项，例如::

    -q：显示消息队列的信息
    -m：显示共享内存的信息
    -s：显示信号量的信息
    -a：显示所有信息（包括消息队列、共享内存和信号量）















