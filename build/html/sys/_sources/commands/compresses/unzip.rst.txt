.. _unzip:

unzip命令
=============


unzip [参数] <压缩文件> [压缩包中将被释放的文件]


* 解压::

    $ unzip FileName.zip
    $ unzip  -o -d /home/sunny myfile.zip
    // -o:不提示的情况下覆盖文件
    // -d:-d /home/sunny 指明将文件解压缩到/home/sunny目录下

* 查看zip文件内容（不解压）::

    $ unzip -v <name>.zip 













