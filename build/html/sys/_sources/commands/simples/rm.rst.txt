.. _rm:

rm命令使用
-------------

rm <目标目录|文件>
删除::

     -r -R 递归删除
     -f 强制删除(无需确认,直接删除。慎用!)
     -i 交互式删除(询问用户)


例-删除所有文件/文件夹(包括隐藏文件.xxx)::

     @todo 下面目录是暂还没有执行成功

     $ rm -rf * ..?* .[!.]*

     说明:
     *         匹配所有非点文件
     .[!.]*    匹配除之外的所有点文件. 和名称以开头的文件..
     ..?*      匹配除以外的所有点文件..




