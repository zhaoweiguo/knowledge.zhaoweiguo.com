mv命令使用
-------------
::

    mv <源文件> <目标目录|文件>

相当于 cp 后删除源文件,也可以作为“重命名”使用::

    mv <源目录> <目标目录>
     -r -R 递归


把隐藏文件(不包括. 和 ..)移动到另一个目录::

    mv .[!.]* /new/folder

    # 如果匹配以两个点开头的文件（如 ..bash_history 等特殊情况）
    mv .[!.]* ..?* /path/to/destination/







