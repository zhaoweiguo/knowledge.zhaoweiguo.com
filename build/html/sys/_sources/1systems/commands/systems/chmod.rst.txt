.. _chmod:

chmod命令使用
====================
::

    chmod <权限表达式> <文件|目录>

更改文件的权限。权限的表达式可以使用三位 8 进制数字表示,或者使用 augo +-= rxw-s来表示::

     -R 递归
     -v 显示过程
     -c 类似“-v”,仅显示更改部分
     –reference=<参考文件或目录> 以指定文件为参考更改权限

示例::

     chmod -R a+x path
     chmod -Rv 755 path
