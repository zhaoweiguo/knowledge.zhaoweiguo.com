make命令
################

命令::

    make [ -f makefile ] [ options ] ... [ targets ] ...

    // 指定makefile文件
    $ make -f rules.txt
    # 或者
    $ make --file=rules.txt


常用参数::

    // 在读取makefile前先切换路径
    -C dir, --directory=dir






