常用
####

上下层系统之间的调用倾向于通过同步接口，同层之间的调用倾向于异步消息调用。因为上下层没有互相调用，只有上层调用下层，调用关系简单。同层之间互相调用，错综复杂，使用中间件异步消息解耦，让调用关系简单。

该不该用这个设计模式，那就看能不能提高代码的可扩展性；要不要重构，那就看重代码是否存在可读、可维护问题等。


设计模式的作用::

    1. 应对面试中的设计模式相关问题
    2. 告别写被人吐槽的烂代码
    3. 提高复杂代码的设计和开发能力
    4. 让读源码、学框架事半功倍
    5. 为你的职场发展做铺垫




